[build-system]
requires = ["hatchling>=1.25.0"]
build-backend = "hatchling.build"

[project]
name = "kya-sdk"
version = "0.1.0"
description = "Know Your Agent Python SDK"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "httpx>=0.28.1",
  "pynacl>=1.5.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.3.0",
]

[tool.hatch.build.targets.wheel]
packages = ["src/kya_sdk"]

[tool.pytest.ini_options]
pythonpath = ["src"]
